import tkinter as tk
from tkinter import ttk
from tkinter.constants import COMMAND
#setting up window
win=tk.Tk()
win.title("Spinbox")
win.resizable(True, True)
win.geometry("450x600+50+50")
win.option_add('*Font', '100')
print(win)

#image setup
label1=tk.Label(win,text="calendario")
image1=tk.PhotoImage(file="merged.gif")
label1.img=image1
label1.config(image=label1.img)
label1.place(x=0,y=0)
label2=tk.Label(win,text="calendario",background="black",foreground="white")
label2.place(x=150,y=300)

def reg():
    print(dia.get)
def reg2():
    print(mes.get)
def reg3():
    print(ano.get)
meses=("janeiro","favereiro","marco","abril","maio","junho","julho","agusto","setembro","outobro","novembro","decembro")


dia=tk.StringVar()

s=tk.Spinbox(win,from_=1,to=31,textvariable=dia)
s.config(state="readonly")
s.bind("selected",reg)#button to initiate recebe_opcao
s.place(x=120,y=370)

mes=tk.StringVar()
s3=ttk.Combobox(win,state="readonly",values=meses,textvariable=mes)
s3.current(1)
s3.bind("selected",reg2)#button to initiate recebe_opcao
s3.place(x=120,y=410)


ano=tk.StringVar()
s2=tk.Spinbox(win,from_=1990,to=2018,textvariable=ano)
s2.config(state="readonly")
s2.bind("selected",reg3)#button to initiate recebe_opcao
s2.place(x=120,y=450)

Button1=tk.Button(win,text="selecionar",background="black",foreground="white",command = lambda:[reg(),reg2(),reg3()])
Button1.place(x=150,y=490)


























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































win.mainloop()